# سجل التغييرات

الصيغة `MAJOR.MINOR.PATCH.BUILD`

| الجزء | متى يتغيّر |
|---|---|
| **MAJOR** | نسخة جديدة بالكامل أو تغيير جذري قد لا يتوافق مع ما قبله |
| **MINOR** | ميزة جديدة متوافقة ولا تعطّل وظيفةً قائمة |
| **PATCH** | إصلاح خطأ أو ثغرة أمنية، بلا ميزة جديدة |
| **BUILD** | بناء يُرفع للمراجعة، أو إصلاح عاجل (Hotfix) |

**القاعدة:** ميزة → ارفع MINOR وصفّر ما بعده · إصلاح → ارفع PATCH وصفّر
BUILD · بناء → ارفع BUILD وحده.

> **لماذا 2.6 لا 1.0:** الإصدار المعلن كان 2.5.0، والنزول إلى 1.0.0.0 يكسر
> التحديث التلقائي — `check_for_updates` يقارن `remote > local`، فعميل
> يحمل 2.5.0 لن يرى 1.x تحديثًا ويبقى عالقًا بلا إشعار. الأرقام تتقدّم ولا
> تتراجع.

---

## 2.12.0.0 — 2026-09-20

### ميزة: بوّابة الرسائل — الإشعار يصل من لا يفتح البوّابة

صندوقُ الإشعارات سجلٌّ **يُقرأ**: يفتح الموظّف البوّابة فيرى ما جدّ.
وهو لا يصل من لم يفتح شيئًا — وهو بالضبط من يحتاج أن يصله. فطلبُ
إجازةٍ ينتظر يومين لا لأنه مرفوض بل لأن المدير لم يفتح «فريقي»،
وبصمةُ التواجد تفوت من لم يدخل النظام أصلًا.

صار لأربعةٍ طريقٌ ثانٍ إلى واتساب:

- **ما يصل واتسابًا:** طلبُ الإجازة، والقرارُ فيه، والاستئذان،
  وتذكيرُ بصمة التواجد.
- **ولا يكلّم هذا التركيبُ واتساب.** يقيّد **وقوعَ حدث** في صندوقٍ
  محلّيّ ويرفعه إلى لوحة onz.one، واللوحةُ تقرّر: أيُرسَل؟ ولمن؟
  وبأيّ قناة؟
- **لا نصَّ رسالةٍ يخرج من المقرّ.** الرسائل تخرج من رقمٍ واحد،
  فالصياغةُ قالبٌ هناك — وما يصل الناسَ باسمه ليس مكتوبًا في مكانٍ
  لا يراه.
- **ولا رقمَ هاتف.** الحدثُ يقول «الموظّف ٨٨»، واللوحة تحلّه من
  البيانات المرفوعة إليها هي. ولو حمل الهاتفَ لصار كلُّ مفتاحٍ
  يُسرَق أداةَ إرسالٍ من ذلك الرقم إلى أيّ رقمٍ في الدنيا.
- **ومفتاحُ خادم الواتساب لا ينزل إلى مقرّ العميل.** من يملكه يملك
  الحساب: إرسالًا، وقراءةً للمحادثات.
- **والانقطاعُ لا يُضيع ولا يُغرق.** الصندوق يحتفظ بما لم يُرفع،
  فتركيبٌ كان مفصولًا يُفرِغه حين يعود. ولكلّ حدثٍ عمرٌ يُقاس من
  **وقوعه** لا من وصوله — فـ«بصمتك مستحقّة الآن» عمرُها ساعتان ولا
  تُرسَل بعد ستّة أيّام.
- **ولا تُرسَل مرّتين.** مفتاحُ تفرّدٍ في الطرفين يحمي من انقطاعٍ
  يقع بين النجاح والردّ عليه.
- مُطفأةٌ افتراضًا: **الإعدادات ← إشعارات واتساب**. ولا حقلَ مفتاحٍ
  فيها — هو نفسُه مفتاح الرفع السحابي.

---

## 2.11.0.0 — 2026-09-19

### ميزة: بوابة العميل السحابية — `client.onz.one`

العميل الذي يُركّب النظام في مقرّه لم يكن يملك شيئًا يراه من خارجه:
لا من هاتفه، ولا محاسبُه من بيته، ولا هو نفسه في سفر. صارت له مساحةٌ
سحابيّة يدخلها بالاسم وكلمة المرور نفسيهما اللذين له على onz.one،
فيرى موظفيه وحضورهم وأجهزته وفروعه.

- **الاتجاه واحد:** من المقرّ إلى السحابة. المقرّ هو المرجع، والسحابة
  مرآةٌ للقراءة — فلا حسمَ تعارضٍ على رواتبَ وحضور
- **يُرفع:** الموظفون، الحضور، الأجهزة، الفروع، الورديّات، الإجازات،
  الرواتب، الأقسام والوظائف والعطل
- **لا يُرفع:** كلمات مرور النظام، ولا مفتاح الترخيص، ولا قوالب
  البصمة الحيويّة، ولا كلمة مرور الموظف على الجهاز — تبقى في المقرّ.
  والحجبُ مزدوج: العميل لا يقرؤها، والخادم يرفضها ولو وصلت
- يُضبط من **الإعدادات ← الرفع السحابي**. والمفتاح لا يُعاد إلى
  الشاشة: حقلٌ فارغ يعني «لم يُغيَّر» لا «امحُه»

### إصلاحات في الرفع — ثلاثة أعطالٍ كانت تفشل صامتةً

الوكيلان السابقان كانا يرشّحان بالطابع الزمني (`created_at`)، وهو
سؤالٌ يفوته ما يقع يوميًّا. حلّ محلَّهما **دفترُ تغييراتٍ**
(`cloud_sync_outbox`) تكتبه مُشغِّلات SQLite، فيسجّل واقعة التغيير
لا زمن الصفّ:

- **التعديل لم يكن يصل.** رفعُ راتبِ موظّف لا يغيّر `created_at`،
  فتبقى السحابة على الراتب القديم إلى الأبد
- **الحذف لم يكن يصل.** موظّفٌ يُحذف من المقرّ يبقى ظاهرًا في السحابة
- **البصمة المتأخّرة كانت تُتخطّى.** جهازٌ كان مفصولًا يُفرِغ بصماتِ
  أمس، و`check_time` لها أقدم من العلامة المائيّة — فلا تصل أبدًا
- **وكيلان ببروتوكولين لا يلتقيان** (`cloud_sync_agent.py` كان يقصد
  نقطةَ نهايةٍ لا وجود لها بمفتاحٍ مكتوبٍ في الشيفرة،
  و`launcher/sync_agent.py` يقصد الحقيقيّة). صارا واحدًا
- **الدفعة لا تُمحى إلا بعد تأكيد الخادم**، فانقطاع الشبكة يُعيدها
  ولا يُضيعها
- **تُصفَّر مؤشّرات الرفع بعد استعادة نسخةٍ احتياطية**، ويُعاد تثبيت
  المُشغِّلات — لأن `DROP TABLE` يُسقطها معه بلا ما يُنبّه

---

## 2.10.59.0 — 2026-09-19

### إصلاحات

- **المسؤول يفتح بوابة الموظف فيرى بيانات موظفٍ آخر بلا أن تقول
  الشاشة ذلك.** مسؤول النظام لا سجلّ موظف له عادةً، فكان يسقط إلى
  «أول موظف نشط» ليرى شكل البوابة — سقوطٌ مقصود وصمتُه ليس. فيرى
  راتبًا وحضورًا وطلباتٍ ليست له: إمّا يقرؤها على أنها بياناته،
  وإمّا يظنّ النظام معطوبًا لأن «بياناته» غريبة عنه. صارت لافتةٌ
  في أعلى البوابة تسمّي من يُستعرَض وتقول إن شيئًا لا يُسجَّل باسمه
- **«متابعة المناديب» كانت مدفونة داخل قائمة «النظام» المطويّة** مع
  الأدوار والمستخدمين والنسخ الاحتياطي، فمن فعّل الوحدة لم يجدها.
  صارت في المستوى الأول من الشريط الجانبي — وهي شاشة تشغيلٍ يوميّة
  لا أداة إدارة — وتختفي بإطفاء الوحدة كما كانت

---

## 2.10.58.0 — 2026-09-18

### إصلاحات

- **استعادة نسخةٍ قديمة كانت تُرجع النظام إلى مخطَّطٍ قديم، فيردّ
  500.** الاستعادة من ملفّ قاعدة تُسقط الجدول القائم وتعيد بناءه
  بمخطَّط الملفّ المرفوع، ولم يكن شيء يُعيد تشغيل الترحيلات بعدها.
  فنسخةٌ من v59 تُسقط `employees.work_mode` و`shift_types.is_split`
  و`fingerprint_devices.branch_id` — و`work_mode` تُقرأ في مسارات
  كثيرة، فكل صفحة تلمسها تصير خطأ 500. صار `init_db` يُنادى بعد كل
  استعادة، وهو نفسه ما يُشغَّل عند كل إقلاع
- ونسخةٌ حديثة لا تُظهر العطب لأن مخطَّطها هو المخطَّط الحالي — فمن
  يجرّب بنسخةٍ أخذها اليوم يراه سليمًا. ولهذا اختبارٌ يحرس الحالين

---

## 2.10.57.0 — 2026-09-18

### إصلاحات

- **مزامنة أوراكل كانت تُحاوَل على كل نظام، ومعظمها لا يستعملها.**
  المفتاح مفتوح افتراضيًّا والمضيف الافتراضي `localhost`، فنظامٌ لم
  يُضبط فيه شيء كان يتصل بـ`localhost:1521` كل خمس دقائق ويسجّل
  `Connection refused` مدى الحياة. صار القياس على «هل ضبط أحدٌ
  مضيفًا أو مستخدمًا — في الإعدادات أو في البيئة»، فلا اتصال ولا
  طابور ولا سطر إلا لمن يستعملها. والسبب يُقال مرّة لا كل خمس دقائق
- والقياس على «هل ضُبط» لا على «أهو localhost»: من يشغّل أوراكل على
  الجهاز نفسه (تركيب محلّي بـXE) مضيفُه `localhost` عن قصد، وقطعُه
  عنه يُعطّل ميزةً يدفع ثمنها
- **وشاشة أوراكل كانت تقول «مفعَّلة» بشارة خضراء** على نظامٍ لا
  يُزامَن فيه شيء. صارت تقول «مفعَّلة — ولم تُضبط بعد»

---

## 2.10.56.0 — 2026-09-17

### إصلاحات

- **ADMS: أوامر لا تصل الجهاز في النشر السحابي.** التعرّف على الجهاز
  يقوم على رقمه التسلسلي أو عنوانه، والعنوان كان يُقرأ من
  `request.remote_addr` الخام. وذاك صحيح في التركيب المحلي حيث يكلّم
  الجهازُ النظامَ مباشرةً؛ أما خلف وسيط (Plesk أو Traefik) فهو عنوان
  الوسيط — واحدًا لكل الأجهزة. فالتعرّف بالعنوان ميت، وجهازٌ مسجَّل
  باسمٍ وصفيّ لا يُطابَق بشيء فلا يُسلَّم إليه أمر. صار يُقرأ
  بـ`utils/net.client_ip` بقدر ما أُعلن من وسطاء
  (`HR_TRUSTED_PROXY_HOPS`)، وأُضيف المتغيّر إلى خدمة ADMS في compose
- **وأسوأ منه: العنوان كان يُكتب في صفوف الأجهزة.** `cdata` يحفظ
  العنوان في `fingerprint_devices` و`pending_adms_devices` — فكان
  عنوان الوسيط يُكتب لكل الأجهزة، و`device_ip` عمودٌ فريد
- **صمتٌ منع التشخيص.** جهازٌ يسأل عن أوامره ولا صفَّ له كان يُجاب
  `OK` بلا سطرٍ واحد في السجلّ. فالمسؤول يضغط «أرسل الوقت»، وتقول له
  الشاشة «تمت الجدولة» بصدق — الأمر كُتب PENDING — ويبقى هناك إلى
  الأبد بلا أن يقول النظام لماذا. صار يُسجَّل برقمه التسلسلي وعدد
  الأوامر المنتظرة وسلسلة `X-Forwarded-For` كما وصلت، مرّةً كل خمس
  دقائق
- **تسعة أبحاث عن الجهاز صارت واحدًا.** كان لكل مسارٍ بحثُه، فيُعرَف
  الجهاز في مسار البصمات ولا يُعرَف في مسار الأوامر. والمُحلِّل
  الموحَّد يثبّت الرقم التسلسلي في اسم الجهاز حين يطابقه بعنوانه،
  فلا يعود معتمدًا على عنوانٍ متغيّر — ولا يحتاج المسؤول أن يُسمّيه
  بنفسه
- كل عميل يسأل اللوحة عن نسخته هو لا عن نسخةٍ للجميع، والمفتاح في
  جسم الطلب لا في سطر العنوان
- `HR_TRUSTED_PROXY_HOPS` كان غائبًا عن compose المستأجرين المولَّد من
  اللوحة، فقيد شبكة الفرع في بوابة الموظفين كان يقارن عنوان الوسيط
  بشبكة الفرع

---

## 2.10.55.1994 — 2026-09-14 · «بوابة الموظف»

### ميزات

- بوابة الموظف: لوحته وحضوره وطلباته وبصمته من حسابه هو
- اعتماد الطلبات من المدير المباشر داخل البوابة
- إدارة الفروع
- العقود بقوالب Word
- معالج الإعداد عند أول تشغيل
- رابط إعادة تعيين كلمة المرور، برمز لمرّة واحدة وصلاحية منتهية

### إصلاحات

- خادم الإنتاج صار waitress بدل خادم Flask التطويري
- رقم الإصدار هنا وفي `utils/version_info.py` كان 2.10.0.0 بينما البناء
  المنشور يحمل 2.10.55.1994. الفارق ليس تسمية: `check_for_updates` يقارن
  المنشور بما يعلنه العميل، فكان كل عميل يرى التحديث نفسه بعد تركيبه
  مباشرةً، وينزّله من جديد في كل فحص، بلا نهاية
- `tools/publish_version.py` كان يقرأ الإصدار من هذا الملف بصيغة `v6.2`
  التي لا يستعملها — فكان يعود بلا نتيجة ويتوقّف قبل أن ينشر شيئًا. صار
  يقرأ الرقم من `utils/version_info.py` وحده، والملاحظات من هنا

---

## 2.10.0.0 — 2026-08-04

### ميزات

- تقرير الأول والأخير مع طباعة كل الموظفين دفعة واحدة

---

## 2.9.0.0 — 2026-08-04

### ميزات

- مصدر بيانات الكشف: البصمات الخام أو الساعات المعتمدة

---

## 2.8.0.0 — 2026-08-04 · «كشف المقاولات»

### ميزات

- كشف قوى عاملة المقاولات بصلاحية مستقلّة

---

## 2.7.5.0 — 2026-08-03

### إصلاحات

- ضبط is_adms تلقائيًا عند اتصال الجهاز — كان يمنع إرسال المستخدمين صامتًا

---

## 2.7.4.0 — 2026-08-02

### إصلاحات

- إرسال وقت اللحظة للجهاز، ومنع تكرار أوامر تحديث المستخدم

---

## 2.7.3.0 — 2026-08-02

### إصلاحات

- استعادة اكتشاف أجهزة ADMS: البوّابة كانت تمنع ظهورها في شاشة الإضافة

---

## 2.7.2.0 — 2026-08-02

### إصلاحات

- استبعاد فحص الصحة من سجل الأجهزة المرفوضة

---

## 2.7.1.0 — 2026-08-02

### إصلاحات

- قبول أجهزة البصمة المسجَّلة بعنوان IP لا بالرقم التسلسلي

---

## 2.7.0.0 — 2026-08-02

### ميزات

- أداة ترقية الإصدار: الرقم والسجل والكوميت معًا

---

## 2.6.0.0 — 2026-08-01 · «الشفت المقسّم والترخيص الموقّع»

### ميزات

- **الشفت المقسّم**: شفت بفترتين أو أكثر بينهما راحة (10-14 ثم 17-22).
  الساعات تُحتسب من مجموع الفترات لا من المدى بينها، وكل فترة توجب
  بصمتين. الفترات متغيّرة لكل شفت لا راحة موحّدة مفروضة.
- **الترخيص الموقّع**: توقيع RSA غير متماثل يعمل أوفلاير بالكامل. المفتاح
  الخاص على الخادم وحده، والعميل يحمل العام فيتحقق ولا يستطيع التوقيع.
- **حدّ الأجهزة داخل الرخصة**: صار بندًا في النصّ الموقَّع بدل إعداد محلي
  يُغيَّر بصفٍّ واحد في قاعدة بيانات العميل.
- **بوّابة قبول في ADMS**: ترفض الأجهزة غير المسجّلة والزائدة عن الحدّ،
  وتسجّل الرفض بسببه.
- **معرّف تركيب ثابت**: محفوظ في مجلد البيانات، فلا يتغيّر بإعادة بناء
  الحاوية، ومعه مسار فكّ ارتباط للنقل إلى خادم جديد.
- **نشر متعدّد العملاء**: أسماء الحاويات ومنافذها من متغيّرات البيئة،
  فعميل جديد = ثلاثة متغيّرات لا تعديل ملف.
- **سجل تدقيق الموظف**: كل تغيير بقيمته القديمة والجديدة وفاعله ومصدره.
- **المعالون والعهد** في ملف الموظف، مع استرجاع عهدة يحفظ التاريخ.
- **طباعة وتصدير Excel** لكشف الرواتب الشهرية.
- **لوحة معلومات** بمؤشّرات ورسوم مبنيّة داخليًّا بلا مكتبة خارجية.
- **تصميم متجاوب** للهواتف: قائمة جانبية منزلقة وأهداف لمس مضبوطة.

### إصلاحات

- **الشفت المقسّم كان يُحتسب بالمدى**: 12 ساعة بدل 9، فتُدفع ثلاث ساعات
  راحة أجرًا وتدخل العمل الإضافي.
- **توقيع الرخصة كان عشوائيًّا** فلا يتفق الطرفان أبدًا ويفشل كل تفعيل.
- **كاش الترخيص لا يُبطل عند التفعيل** فتعود الصفحة إلى شاشة الترخيص رغم
  نجاحه.
- **معرّف الجهاز كان يُشتقّ من عنوان MAC** الذي تولّده Docker عشوائيًّا عند
  كل بناء، فتُرفض رخصة عميل عامل.
- **حدّ الأجهزة لم يكن مفروضًا في ADMS** — وهو المسار الذي تصل منه البصمات.
- **ثمانية مفاتيح ترجمة** كانت تُعرض خامًا للمستخدم.
- **فحص الصحة كان واحدًا للخدمات الثلاث** فيُظهر خادم البصمة معطّلًا وهو
  سليم.
- **رصيد الإجازات كان يتراكم من تاريخ التعيين** بلا سجلّ لما أُخذ فعلًا،
  فينتج رصيدًا وهميًّا يتحوّل إلى مال في نهاية الخدمة.
- **الاستيراد كان يفشل صامتًا** على الموظف الموجود بدل تحديثه، ويحوّل
  الأرقام الوظيفية إلى كسور عشرية فيكسر مطابقة البصمة.
- **شاشة الرواتب القديمة** كانت تكتب في جدول لا يقرأه محرّك الرواتب.

### أمان

- الربط الافتراضي على المضيف المحلي، والكشف على الشبكة قرار صريح.
- كلمة مرور أوراكل تُخزَّن مشفَّرةً بمفتاح مربوط بالجهاز.
- استبعاد السجلّات والمفاتيح من صورة Docker.
- منع الحلقات المغلقة وتعيين الموظف مديرًا لنفسه في التسلسل الإداري.

---

## 2.5.0 وما قبله

سُجّلت في تاريخ المستودع. هذا الملف يبدأ من 2.6.0.0.
