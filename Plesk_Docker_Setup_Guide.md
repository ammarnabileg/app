# دليل تشغيل النظام على سيرفر Plesk باستخدام Docker

تم تجهيز هذه النسخة لتعمل بشكل كامل ومستقل على أي سيرفر يدعم Docker (مثل سيرفرات Plesk أو Ubuntu).

## الملفات المهمة في هذه النسخة:
- `Dockerfile`: يحتوي على تعليمات بناء بيئة Python المطلوبة.
- `docker-compose.yml`: لتشغيل النظام كخدمتين (خدمة الويب للموقع `hr_web` وخدمة المزامنة للبصمات `hr_sync`).
- `utils/db.py`: تم تعديله ليحفظ قاعدة البيانات داخل مسار `/app/data` الخاص بـ Docker، مع توافق كامل إذا تم تشغيله على Windows.

---

## خطوات التشغيل عبر لوحة تحكم Plesk:

### 1. تجهيز الملفات:
قم بضغط المجلد الحالي (`hr_python_app_docker`) إلى ملف ZIP، وارفعه إلى مسار مناسب في السيرفر الخاص بك من خلال File Manager في Plesk (مثلاً: `/var/www/vhosts/yourdomain.com/hr_system/`).

### 2. تثبيت إضافة Docker في Plesk:
إذا لم تكن إضافة Docker مفعلة لديك، اذهب إلى قائمة **Extensions** في لوحة Plesk وابحث عن **Docker** وقم بتثبيته.

### 3. استخدام Docker Compose (الطريقة الأسهل):
بما أننا أنشأنا ملف `docker-compose.yml`، فإن أفضل طريقة لتشغيل النظام هي عبر موجه الأوامر (SSH Terminal) في Plesk.
1. افتح الـ **SSH Terminal** من لوحة Plesk.
2. انتقل إلى المجلد الذي رفعت فيه الملفات:
   ```bash
   cd /var/www/vhosts/yourdomain.com/hr_system/
   ```
3. قم بتشغيل الأمر التالي لبناء وتشغيل الحاويات:
   ```bash
   docker-compose up -d --build
   ```

هذا الأمر سيقوم بـ:
- تحميل بيئة Python.
- تثبيت المكتبات المطلوبة.
- تشغيل خادم الويب على البورت `5000`.
- تشغيل سكريبت المزامنة `fingerprint_sync.py` في الخلفية ليقوم بسحب البصمات تلقائياً.
- إنشاء مساحة (Volume) لحفظ قاعدة البيانات `hr_system.db` حتى لا تضيع البيانات عند إعادة تشغيل السيرفر.

### 4. ربط الدومين بالبورت وإعداد الـ HTTPS (Reverse Proxy):
لكي يعمل البرنامج على رابط موقعك الآمن (HTTPS) بدون الحاجة لكتابة البورت (`https://yourdomain.com` بدلاً من `http://yourdomain.com:5000`):

1. **تفعيل شهادة الأمان (SSL):**
   - من لوحة Plesk، اذهب إلى الدومين الخاص بك.
   - اختر **SSL/TLS Certificates** وقم بتفعيل شهادة مجانية (Let's Encrypt).
   - تأكد من تفعيل خيار **Force SSL/TLS** (لإجبار توجيه الزوار إلى HTTPS).

2. **ربط الويب (المنفذ 5000):**
   - في إعدادات الدومين الخاص بك في Plesk، اذهب إلى **Docker Proxy Rules**.
   - اضغط على **Add Rule**.
   - **URL:** اتركها فارغة (أو ضع `/` لتمثل الصفحة الرئيسية).
   - **Container:** اختر `hr_web_app`.
   - **Port:** اختر `5000`.
   - احفظ التغييرات. الآن الموقع سيعمل بشكل آمن على الدومين الرئيسي.

3. **إعداد مسار أجهزة البصمة ADMS (المنفذ 8081):**
   بما أن أجهزة البصمة تتصل عادةً عبر مسار `/iclock/`، سنقوم بعمل قاعدة إضافية لها لكي تمر عبر نفس الدومين بشكل صحيح:
   - في نفس صفحة **Docker Proxy Rules**، اضغط على **Add Rule** مجدداً.
   - **URL:** اكتب `/iclock`
   - **Container:** اختر `hr_adms_server`.
   - **Port:** اختر `8081`.
   - احفظ التغييرات.
   *(بهذه الطريقة، إذا كان الدومين هو `hr.company.com`، فإن الويب سيعمل على الدومين الرئيسي، بينما أجهزة البصمة ستتصل عبر الرابط `hr.company.com/iclock` وسيقوم Plesk بتوجيهها مباشرة إلى سيرفر ADMS).*

### 5. إعدادات جدار الحماية (Firewall) في السيرفر:
إذا كنت ترغب في أن تتصل أجهزة البصمة مباشرة بالـ IP العام وبورت 8081 (مثلاً `http://1.2.3.4:8081`):
تأكد من الذهاب إلى إعدادات جدار الحماية في السيرفر (Plesk Firewall أو إعدادات مزود الخدمة) والسماح بالاتصالات الواردة (Inbound) على البورت **8081**.

---
**ملاحظة فنية:** 
لا تقم بمسح مسار الـ Volume المنشأ في الدوكر (`hr_data`) لأنه يحتوي على ملف `hr_system.db` الذي يحفظ جميع بيانات الموظفين والبصمات والرواتب.
