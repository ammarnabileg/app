# متطلبات الخادم. هذا الملف وحده ما يُثبَّت داخل صورة Docker لكل مستأجر.
#
# مكتبات الواجهة الرسومية (customtkinter وPillow وpystray) ليست هنا عمدًا:
# يحتاجها launcher.py على ويندوز فقط، وإدخالها هنا يحشو صورة كل عميل
# بمكتبات رسومية لا تعمل أصلًا في حاوية بلا شاشة. مكانها
# requirements-desktop.txt.
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.6.3
pyzk==0.9.0
openpyxl>=3.1.5
cryptography>=41.0.0
oracledb
requests
flask_babel
pandas
# خادم إنتاج بدل خادم التطوير المدمج في Flask
waitress>=3.0.0
# قوالب عقود Word
docxtpl
# التحقّق من بصمات bcrypt الآتية من لوحة onz.one ('$2y$')؛
# werkzeug لا تفهم هذه الصيغة.
bcrypt>=4.0.0
